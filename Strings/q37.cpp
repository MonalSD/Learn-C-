// Write a program to Find the shortest word in a string.
#include<iostream>
#include<string>
using namespace std;

int main()
{
    string str;
    cout<<"Enter string: ";
    getline(cin,str);
    int len=str.length();
    for(int i=0;i<len;i++)
    {
        if(str[i]==" ")
        {
            
        }
    }
}