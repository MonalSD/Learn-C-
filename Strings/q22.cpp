// Write a program to find the frequency of characters in a string.
#include<iostream>
#include<string>
using namespace std;

int main()
{
   string str;
   cout<<"Enter a string: ";
   getline(cin,str);
   int len=0;
   while(str[len]!='\0')
   {
       len++;
   } 
   int i;
   for(i=0;i<len;i++)
   {
       
   }

}