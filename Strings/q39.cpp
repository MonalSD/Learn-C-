// Write a Program to Find words ending with given characters(suffix).
#include<iostream>
#include<string>
using namespace std;

int main()
{
    string str;
    char ch;
    cout<<"Enter String: ";
    getline(cin,str);
    int len=str.length();
    cout<<"Enter suffix:";
    cin>>ch;


}