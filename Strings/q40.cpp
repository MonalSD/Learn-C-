// Write a program to Find words Starting with given characters(Prefix).
#include<iostream>
#include<string>
using namespace std;

int main()
{
    string str;
    char ch;
    cout<<"Enter String: ";
    getline(cin,str);
    int len=str.length();
    cout<<"Enter prefix:";
    cin>>ch;


}